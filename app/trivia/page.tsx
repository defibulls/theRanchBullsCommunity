import React from "react";
import Header from "../Header";

type Props = {};

const Home = async (props: Props) => {
  return (
    <div className="">
      <Header notLanding={false} />
    </div>
  );
};

export default Home;
